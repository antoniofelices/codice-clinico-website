---
import { Circle } from "@lucide/astro"

interface Props {
  variant?: "default" | "on-dark"
  class?: string
  size?: string
}

const { variant = "default", class: extraClass = "", size = "14" } = Astro.props
---

<Circle
  class:list={[
    "eyebrow rounded-full",
    variant === "default"
      ? "text-sky-50 bg-sky-50 shadow-sky-50"
      : "text-cream-50 bg-cream-50 shadow-cream-50",
    extraClass,
  ]}
  aria-hidden="true"
  size={size}
/>
